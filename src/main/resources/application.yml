# OAuth
spring:
  security.oauth2.client:
    registration:
      google:
       client-id: ${GOOGLE_CLIENT-ID}
       client-secret: ${GOOGLE_CLIENT-SECRET}
      github:
        client-id: ${GITHUB_CLIENT-ID}
        client-secret: ${GITHUB_CLIENT-SECRET}
